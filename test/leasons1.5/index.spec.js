const algo = require('../../leasons1.5');

test('concat numbers', () => {
  expect(algo.concatNum(10)).toBe(9);
});

test('count j numbers < of i', () => {
  expect(algo.concatJNum(10)).toBe(120);
});
